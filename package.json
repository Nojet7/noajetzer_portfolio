{
  "name": "portfolio-noa-jetzer",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "postbuild": "rimraf docs && cpx \".output/public/**/*\" docs && shx touch docs/.nojekyll && shx echo . > docs/.nojekyll && shx echo noajetzer.ch > docs/CNAME"
  },
  "dependencies": {
    "cpx": "^1.5.0",
    "nuxt": "^4.2.2",
    "rimraf": "^6.1.3",
    "shx": "^0.4.0",
    "vue": "^3.5.26",
    "vue-router": "^4.6.4"
  }
}
